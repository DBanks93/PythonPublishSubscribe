from setuptools import setup, find_packages

setup(
    name='pythonpublishsubscribe',
    version='0.0.1',
    description='Python Publish Subscribe Framework',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.12',
)